import {expect} from 'chai';
import {GameState, Game, Frame, Turn, Player} from '../src/bowling';

describe('something', () => {
    it('yup', () => {
        let player = new Player();

        expect(13).to.eq(13);
    });
});